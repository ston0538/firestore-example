{
  "name": "firestore-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "emulator": "firebase emulators:start",
    "emulator:import": "firebase emulators:start --import=./firestore_export --export-on-exit",
    "emulator:backup": "firebase emulators:export ./firestore_export"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "firebase": "^8.2.0"
  }
}